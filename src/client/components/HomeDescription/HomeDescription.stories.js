import React from 'react';
import HomeDescription from './HomeDescription';

export default {
  title: 'Components /Home description card',
  component: HomeDescription,
};

export const component = () => <HomeDescription />;
